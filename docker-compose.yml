version: '3.8'

services:
  metadata-enhancer:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
    # Load .env file if present
    env_file:
      - .env
